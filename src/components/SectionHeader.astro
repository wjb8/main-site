---
import "../styles/components/SectionHeader.scss";

interface Props {
  title: string;
  size?: 1 | 2 | 3 | 4 | 5 | 6;
}

const { title, size = 2 } = Astro.props;

const Tag = `h${size}` as keyof HTMLElementTagNameMap;
---

<Tag class={`section-header section-header-${size}`}>
  {title}
</Tag>
